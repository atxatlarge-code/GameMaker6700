## 2026-05-26 - Forms and Icon Buttons Accessibility\n**Learning:** The application's main overlays (New Thread and Cloud Settings) use standard HTML forms but initially lacked proper `for` attributes on labels and `aria-label` attributes on input elements and icon-only buttons like the scroll controls.\n**Action:** Always ensure that when modifying or adding form elements and icon buttons in the `index.html` overlays, explicit associations and ARIA labels are included to support screen readers and keyboard navigation.
